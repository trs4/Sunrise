using Sunrise.Model.Communication.Data

namespace Sunrise.Model.Communication

service SyncService with lz4, protected {
    streaming SubscriptionTicket Subscription(ConnectParameters parameters)
    Empty TransferMediaLibrary(MediaLibraryData data)
    Empty TransferMediaFiles(MediaFilesData data)
}
